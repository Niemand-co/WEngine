extern WEngine::Application* WEngine::CreateApplication();


int main(int argc, char** argv)
{
	//WEngine::Application* g_pApp = WEngine::CreateApplication();
	//g_pApp->Tick();

	//WEngine::WHashMap<WEngine::WString, int> map;

	//map.Insert("one", 1);
	//map.Insert("two", 2);
	//if(map.Find("one"))
	//	RE_LOG("Has One");

	//map.Erase("one");
	//if (map.Find("one"))
	//	RE_LOG("Has One");

	return 0;
}